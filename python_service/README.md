# Spoilage Prediction Service (Python)

This is a minimal FastAPI service that accepts a POST /predict request and returns a spoilage risk score. It's intended to be expanded with climate data fetching and ML models.
